// app.eop.bg (ЦАИС ЕОП) — скрапер за ПЪЛНОТО досие на поръчка.
// Чуплив по дизайн, затова се вика САМО за вече филтрирани (релевантни) поръчки,
// когато трябва документ, който TED не дава. Подаването на оферта изисква КЕП → остава ръчно.
//
// TODO (Фаза 3, по нужда): за повечето поръчки TED вече дава BG PDF + XML, така че
// този адаптер вероятно ще трябва само за документация, която не е в TED.

export async function fetchEopDossier(tender) {
  // Placeholder — за момента ползваме готовите TED линкове (tender.pdfBg / tender.xml).
  return { documents: [], note: "ЦАИС ЕОП скрапер не е имплементиран — ползвай TED линковете" };
}
